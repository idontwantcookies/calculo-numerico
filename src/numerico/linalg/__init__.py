from .core import (LU, Cholesky, square, simmetrical, is_lower_trig, 
                   is_upper_trig, successive_substitutions, 
                   retroactive_substitutions, det, decomp, quicksolve, solve,
                   inv)
